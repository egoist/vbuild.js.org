import pkg from './package'

export default {
  title: 'vbuild: Install once, Build everywhere',
  template: './index.html',
  resolve: true,
  vendor: [
    'eva.js',
    'in-view',
    'jump.js'
  ]
}
